[pytest]
# AI Agent Assistant System — 测试配置
asyncio_mode = auto
testpaths = tests
python_files = test_*.py

markers =
    slow: 涉及 LLM 调用的慢测试（使用 -m "not slow" 跳过）
    ws:   WebSocket 测试

addopts = -v --tb=short --strict-markers
